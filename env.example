# Socket.IO Server Environment Variables

# Port for the server (Railway will set this automatically)
PORT=4000

# CORS origins (comma-separated)
# For development: http://localhost:3000
# For production: Add your production URL (e.g., https://your-app.vercel.app)
SOCKET_IO_CORS_ORIGIN=http://localhost:3000

# Database connection string (Required for Prisma)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/pet_sitter_db?schema=public"